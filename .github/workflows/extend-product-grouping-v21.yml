name: Extend MB1 Product Grouping V21

# Disabled from automatic push runs on 2026-09-20.
# The grouping expansion is already installed. Run manually only if it is intentionally needed again.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MB1 Product Grouping V21 is retained for manual reference only."
